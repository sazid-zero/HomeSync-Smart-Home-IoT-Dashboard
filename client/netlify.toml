[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application routing config
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
